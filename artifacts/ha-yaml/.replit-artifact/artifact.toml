kind = "web"
previewPath = "/"
title = "Home Assistant YAML Studio"
version = "1.0.0"
id = "artifacts/ha-yaml"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19970

[services.development]
run = "pnpm --filter @workspace/ha-yaml run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ha-yaml", "run", "build" ]
publicDir = "artifacts/ha-yaml/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19970"
BASE_PATH = "/"
